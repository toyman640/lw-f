!(function (n) { typeof define === 'function' && define.amd ? define(['jquery'], (e) => n(e)) : typeof module === 'object' && typeof module.exports === 'object' ? exports = n(require('jquery')) : n(jQuery); }((n) => {
  function e(n) { const e = 7.5625; const t = 2.75; return n < 1 / t ? e * n * n : n < 2 / t ? e * (n -= 1.5 / t) * n + 0.75 : n < 2.5 / t ? e * (n -= 2.25 / t) * n + 0.9375 : e * (n -= 2.625 / t) * n + 0.984375; } void 0 !== n.easing && (n.easing.jswing = n.easing.swing); const t = Math.pow; const u = Math.sqrt; const r = Math.sin; const i = Math.cos; const a = Math.PI; const c = 1.70158; const o = 1.525 * c; const s = 2 * a / 3; const f = 2 * a / 4.5; n.extend(n.easing, {
    def: 'easeOutQuad', swing(e) { return n.easing[n.easing.def](e); }, easeInQuad(n) { return n * n; }, easeOutQuad(n) { return 1 - (1 - n) * (1 - n); }, easeInOutQuad(n) { return n < 0.5 ? 2 * n * n : 1 - t(-2 * n + 2, 2) / 2; }, easeInCubic(n) { return n * n * n; }, easeOutCubic(n) { return 1 - t(1 - n, 3); }, easeInOutCubic(n) { return n < 0.5 ? 4 * n * n * n : 1 - t(-2 * n + 2, 3) / 2; }, easeInQuart(n) { return n * n * n * n; }, easeOutQuart(n) { return 1 - t(1 - n, 4); }, easeInOutQuart(n) { return n < 0.5 ? 8 * n * n * n * n : 1 - t(-2 * n + 2, 4) / 2; }, easeInQuint(n) { return n * n * n * n * n; }, easeOutQuint(n) { return 1 - t(1 - n, 5); }, easeInOutQuint(n) { return n < 0.5 ? 16 * n * n * n * n * n : 1 - t(-2 * n + 2, 5) / 2; }, easeInSine(n) { return 1 - i(n * a / 2); }, easeOutSine(n) { return r(n * a / 2); }, easeInOutSine(n) { return -(i(a * n) - 1) / 2; }, easeInExpo(n) { return n === 0 ? 0 : t(2, 10 * n - 10); }, easeOutExpo(n) { return n === 1 ? 1 : 1 - t(2, -10 * n); }, easeInOutExpo(n) { return n === 0 ? 0 : n === 1 ? 1 : n < 0.5 ? t(2, 20 * n - 10) / 2 : (2 - t(2, -20 * n + 10)) / 2; }, easeInCirc(n) { return 1 - u(1 - t(n, 2)); }, easeOutCirc(n) { return u(1 - t(n - 1, 2)); }, easeInOutCirc(n) { return n < 0.5 ? (1 - u(1 - t(2 * n, 2))) / 2 : (u(1 - t(-2 * n + 2, 2)) + 1) / 2; }, easeInElastic(n) { return n === 0 ? 0 : n === 1 ? 1 : -t(2, 10 * n - 10) * r((10 * n - 10.75) * s); }, easeOutElastic(n) { return n === 0 ? 0 : n === 1 ? 1 : t(2, -10 * n) * r((10 * n - 0.75) * s) + 1; }, easeInOutElastic(n) { return n === 0 ? 0 : n === 1 ? 1 : n < 0.5 ? -(t(2, 20 * n - 10) * r((20 * n - 11.125) * f)) / 2 : t(2, -20 * n + 10) * r((20 * n - 11.125) * f) / 2 + 1; }, easeInBack(n) { return (c + 1) * n * n * n - c * n * n; }, easeOutBack(n) { return 1 + (c + 1) * t(n - 1, 3) + c * t(n - 1, 2); }, easeInOutBack(n) { return n < 0.5 ? t(2 * n, 2) * (7.189819 * n - o) / 2 : (t(2 * n - 2, 2) * ((o + 1) * (2 * n - 2) + o) + 2) / 2; }, easeInBounce(n) { return 1 - e(1 - n); }, easeOutBounce: e, easeInOutBounce(n) { return n < 0.5 ? (1 - e(1 - 2 * n)) / 2 : (1 + e(2 * n - 1)) / 2; },
  });
}));
